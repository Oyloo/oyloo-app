# Tasks — implementation plan

**Spec:** `docs/superpowers/specs/2026-09-23-tasks-design.md`

**Tests:** `swift test` from the repo root. **Build:** `xcodebuild … CODE_SIGNING_ALLOWED=NO`; signed build on the Mini.

### Task 1: Core
- Extend `TaskItem` (`waitingOn`, `tags`, tolerant decoding) and `TasksOverview` (`tag`, `tags`, `circles`).
- `TaskDraft`, `TaskView`, `DueWording`, `statusActions(for:)`, `createBody(…)`, `TaskErrorCode`.
- Tests first; commit `feat(core): task views, due wording and drafts`.

### Task 2: API
- `tasks(view:tag:)`, `setTaskStatus(id:status:)`, `createTask(_:)`, `parseTask(text:)` returning `TaskDraft`; `send` returns data.
- Commit `feat(app): task actions in the API client`.

### Task 3: Screens
- `Sources/App/Tasks/{TasksView,TaskRow,NewTaskSheet,TaskWords}.swift`; per-view resources in `FamilyData`.
- Build; commit `feat(app): task views, swipes and voice quick-add`.

### Task 4: Strings and delivery
- ru/lv for new keys; commit. Signed build on the Mini, install on Ed's phone; merge to `main` after Ed checks.
